package com.loonds.scan.util;

public class QRCodeGenerator {
//    public static byte[] generateQRCodeImage(String data, int width, int height) {
//        return QRCode.from(data).withSize(width, height).to(ImageType.PNG).stream().toByteArray();
//    }
}
